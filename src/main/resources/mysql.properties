getDetailsAfterLogin = select tup.*, cu.APP_CLIENT_ID, tc.NAME, cu.AC_C_USER_ID, cu.COMPANY_ID from tb_company_user cu left join tb_company tc on cu.COMPANY_ID = tc.id left join tb_user_profile tup on cu.USER_ID = tup.USER_ID where cu.user_id = ?